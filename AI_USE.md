# AI Use Log

| Tool Used | What I Asked | What I Kept/Changed/Rejected |
| --- | --- | --- |
| GitHub Copilot Chat | Write a clean_data function to handle messy price (₹, commas), discount percentage (%), rating (non-numeric placeholder), and rating_count (commas, missing values) columns | Kept the function as-is after reviewing the logic. Confirmed the strip-then-convert order of operations (astype(str) then str.replace then pd.to_numeric with errors="coerce"). Deliberately kept the dropna(how="any") approach, which drops a row if any of the five cleaned columns is missing, prioritizing clean/reliable data over maximizing row count. |
| GitHub Copilot Chat | Add top-level code to app.py that loads data/raw/amazon.csv, runs it through clean_data, and prints before/after/dropped row counts (for a Streamlit app, not wrapped in a main function) | Kept the logic, but first had to correct Copilot's initial attempt which wrapped the code in a main() function — that pattern doesn't work for Streamlit, since Streamlit runs files top to bottom rather than through a traditional entry point. Reissued the prompt specifying "top level, not inside main" and the second version was correct. |
| GitHub Copilot Chat | Add a Streamlit page title, one-sentence description, and preview table of the first several rows of cleaned_df, at the top level of app.py | Kept the title, description, and st.dataframe preview table. Removed the st.write row-count lines myself afterward, since that debug info wasn't meant for the end user viewing the dashboard. |
| GitHub Copilot Chat | Add 3 summary metrics using st.metric to app.py: total products, average rating, and average discount percentage | Kept as-is. Used st.columns(3) to display them side by side, and reused the existing after_rows variable for the total product count. |
